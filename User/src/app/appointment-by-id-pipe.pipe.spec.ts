import { AppointmentByIdPipePipe } from './appointment-by-id-pipe.pipe';

describe('AppointmentByIdPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AppointmentByIdPipePipe();
    expect(pipe).toBeTruthy();
  });
});
